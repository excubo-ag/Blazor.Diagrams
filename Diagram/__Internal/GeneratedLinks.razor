
<CascadingValue Name="@(nameof(LinkBase.IsInternallyGenerated))" Value="true" IsFixed="true">
    @foreach (var link in Links)
    {
        if (link.Type == null)
        {
            <Link @key="(link.Source, link.Target)" Source="link.Source" Target="link.Target" OnCreate="link.OnCreate" Arrow="link.Arrow" Type="link.LinkType" />
        }
        else
        {
            <LinkByType Data="link" />
        }
    }
</CascadingValue>

@code{
    [Parameter] public List<LinkData> Links { get; set; }
    internal void TriggerStateHasChanged() => StateHasChanged();
}